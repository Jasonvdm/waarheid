To run this translator on the development set cd into the code directory and run:
python Translate.py dev_set.txt

To run this translator on the test set, cd into code directory and run:
python Translate.py test_set.txt
